# Atualização do Chakra UI

## Objetivo
Atualizar a versão da biblioteca `@chakra-ui/react` para a `3.34.0`, de acordo com a solicitação.

## Alterações
- Atualizado o arquivo `package.json` em `frontend/` definindo a versão do `@chakra-ui/react` para `^3.34.0`.
- Executado `npm install` no diretório `frontend/` para atualizar o `package-lock.json` e as dependências.

## Endpoints afetados (se houver)
- N/A

## Regras de negócio
- N/A

## Observações
- A atualização do Chakra UI pode envolver quebras de compatibilidade com a versão 2.x anterior, sendo recomendada a leitura do guia de migração do Chakra UI v3.
